## How to Run This Project.

    Clone Repository:

    https://github.com/kaitcham/ai-software-engineer-assignment.git

### Local Environment

1. Install dependencies: `npm install`
2. Run tests: `npm test`

### Docker Environment

1. Build the image: `docker build -t ai-assignment . `
2. Run the tests: `docker run --rm ai-assignment`
